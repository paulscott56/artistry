/** This class was generated by GenTest@Mobacar */
package artistry.webhook;

import artistry.models.WebHookMessage;
import org.apache.commons.lang3.SerializationUtils;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.MockitoAnnotations;
import org.mockito.runners.MockitoJUnitRunner;

import static org.junit.Assert.assertNotNull;

@RunWith(MockitoJUnitRunner.class)
public class MessageReceivedEventTest {

	/**
	 * If the class has a default constructor, then you don't need to instantiate it manually. InjectMocks annotation will do it.<br/>
	 * Otherwise use the {@link #setupTest()} method for creating the underTest object.
	 */
	@InjectMocks
	private MessageReceivedEvent underTest;

	@Before
	public void setupTest() {
		// prepare test
		MockitoAnnotations.initMocks(this);
	}

	@Test
	public void testGetWebHookMessage() throws Exception {
		// given
		// when
		WebHookMessage actual = underTest.getWebHookMessage();
		// then
		assertNotNull(actual);
	}

	@Test
	public void testSerialization() throws Exception {
		// given
		// when
		MessageReceivedEvent actual = SerializationUtils.clone(underTest);
		// then
		assertNotNull(underTest.getWebHookMessage() + " was the original value", actual.getWebHookMessage());
	}
}
