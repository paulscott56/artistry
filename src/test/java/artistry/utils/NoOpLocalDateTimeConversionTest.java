/** This class was generated by GenTest@Mobacar */
package artistry.utils;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.MockitoAnnotations;
import org.mockito.runners.MockitoJUnitRunner;

import java.time.LocalDateTime;

import static org.junit.Assert.assertNotNull;

@RunWith(MockitoJUnitRunner.class)
public class NoOpLocalDateTimeConversionTest {

	/**
	 * If the class has a default constructor, then you don't need to instantiate it manually. InjectMocks annotation will do it.<br/>
	 * Otherwise use the {@link #setupTest()} method for creating the underTest object.
	 */
	@InjectMocks
	private NoOpLocalDateTimeConversion underTest;

	@Before
	public void setupTest() {
		// prepare test
		MockitoAnnotations.initMocks(this);
	}

	@Test
	public void testToEntityAttribute() throws Exception {
		// given
		LocalDateTime value = null;
		// when
		LocalDateTime actual = underTest.toEntityAttribute(value);
		// then
		assertNotNull(actual);
	}

	@Test
	public void testToGraphProperty() throws Exception {
		// given
		LocalDateTime value = null;
		// when
		LocalDateTime actual = underTest.toGraphProperty(value);
		// then
		assertNotNull(actual);
	}

}
