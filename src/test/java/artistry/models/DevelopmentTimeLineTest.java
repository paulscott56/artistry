/** This class was generated by GenTest@Mobacar */
package artistry.models;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.MockitoAnnotations;
import org.mockito.junit.MockitoJUnitRunner;

import java.util.Date;

import static org.junit.Assert.assertEquals;

@RunWith(MockitoJUnitRunner.class)
public class DevelopmentTimeLineTest {

	/**
	 * If the class has a default constructor, then you don't need to instantiate it manually. InjectMocks annotation will do it.<br/>
	 * Otherwise use the {@link #setupTest()} method for creating the underTest object.
	 */
	@InjectMocks
	private DevelopmentTimeLine underTest;

	@Before
	public void setupTest() {
		// prepare test
		MockitoAnnotations.initMocks(this);
	}

	@Test
	public void testGetEstimatedCompletionDate() throws Exception {
		// given
		Date value = new Date();
		underTest.setEstimatedCompletionDate(value);
		// when
		Date actual = underTest.getEstimatedCompletionDate();
		// then
		assertEquals(value, actual);
	}

	@Test
	public void testGetEstimatedStartDate() throws Exception {
		// given
		Date value = new Date();
		underTest.setEstimatedStartDate(value);
		// when
		Date actual = underTest.getEstimatedStartDate();
		// then
		assertEquals(value, actual);
	}

	@Test
	public void testGetId() throws Exception {
		// given
		Long value = 0L;
		underTest.setId(value);
		// when
		Long actual = underTest.getId();
		// then
		assertEquals(value, actual);
	}

}
