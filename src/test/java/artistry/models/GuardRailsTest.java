/** This class was generated by GenTest@Mobacar */
package artistry.models;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.MockitoAnnotations;
import org.mockito.junit.MockitoJUnitRunner;

import java.math.BigDecimal;

import static org.junit.Assert.assertEquals;

@RunWith(MockitoJUnitRunner.class)
public class GuardRailsTest {

	/**
	 * If the class has a default constructor, then you don't need to instantiate it manually. InjectMocks annotation will do it.<br/>
	 * Otherwise use the {@link #setupTest()} method for creating the underTest object.
	 */
	@InjectMocks
	private GuardRails underTest;

	@Before
	public void setupTest() {
		// prepare test
		MockitoAnnotations.initMocks(this);
	}

	@Test
	public void testGetId() throws Exception {
		// given
		Long value = 0L;
		underTest.setId(value);
		// when
		Long actual = underTest.getId();
		// then
		assertEquals(value, actual);
	}

	@Test
	public void testGetLowerLimit() throws Exception {
		// given
		BigDecimal value = BigDecimal.valueOf(0);
		underTest.setLowerLimit(value);
		// when
		BigDecimal actual = underTest.getLowerLimit();
		// then
		assertEquals(value, actual);
	}

	@Test
	public void testGetUpperLimit() throws Exception {
		// given
		BigDecimal value = BigDecimal.valueOf(0);
		underTest.setUpperLimit(value);
		// when
		BigDecimal actual = underTest.getUpperLimit();
		// then
		assertEquals(value, actual);
	}

}
